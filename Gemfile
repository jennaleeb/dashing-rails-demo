source  'http://rubygems.org'
ruby    '2.2.2'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.0'

gem 'sass-rails', '~> 4.0.0'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.0.0'
gem 'jquery-rails'

# environment variables
gem 'figaro'

# gem 'shopify_app' (not sure which one ^^ to use)
gem 'shopify_api'

gem 'shopify_cli'
gem 'omniauth-shopify-oauth2'

gem 'twitter'

group :production do
  gem 'pg'
  gem 'rails_12factor'
end

group :development do
  gem 'sqlite3'
end

gem 'dashing-rails'
gem 'puma'
